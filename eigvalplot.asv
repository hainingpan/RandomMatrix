% for i=1:100
i=1;
% H1=H{end};
% W1=W{end};
% H1=hh;
% W1=ww;
% Heff=heff(H1,W1);
% eiglist=eig(Heff);
figure;
% scatter(real(eiglist(:)),imag(eiglist(:)),'.');
scatter(real(eigroot(:,:)),imag(eigroot(:,:)),'.');

grid
xlim([-1,1])
if hm>vm
    title(strcat("\alpha_1=",num2str(alpha2list(i)),"\alpha_2=",num2str()))
else
    title(strcat("\alpha_1=",num2str(alpha1list(vpos)),"\alpha_2=",num2str(alpha2list(i))))
end
[flag,~,~,im]=isimag(H1,W1);
if flag==1
    disp(im);
end
% end